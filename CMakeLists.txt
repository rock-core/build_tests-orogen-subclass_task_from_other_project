PROJECT(subclass_task_from_other_project)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/.orogen/config")
INCLUDE(subclass_task_from_other_projectBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

